<h1>我的所有料理</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Description</th>
      <th>User</th>
      <th>Photo</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @recipes.each do |recipe| %>
      <tr>
        <td><%= recipe.name %></td>
        <td><%= recipe.description %></td>
        <td><%= recipe.user_id %></td>
        <td><%= recipe.photo_id %></td>
        <td><%= link_to 'Show', recipes_path(recipe) %></td>
        <td><%= link_to 'Edit', edit_user_recipe_path(current_user, recipe) %></td>
        <td><%= link_to 'Destroy', user_recipe_path(current_user, recipe), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Recipe', new_user_recipe_path %>
